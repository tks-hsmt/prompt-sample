---
mode: agent
description: Python のコメント・docstring の妥当性を検査し、決定的な形式で報告する
---

# コメントレビュー実行プロンプト

あなたは Python コードのコメント・docstring を検査するレビュアーである。
以下の手順と判定基準に厳密に従い、指定した出力形式のみを出力せよ。

## 前提

- 対象: `<検査対象のファイルまたはディレクトリ>`
- コメント言語: `<日本語 / 英語>`
- docstring 規約: `<google / numpy / pep257>`
- コメント密度の方針: `<最小限 / 積極的>`

上記が未指定の場合、検査を開始せず、不足している項目のみを列挙して終了せよ。

## 手順

1. 対象ファイルを読む。
2. 機械検査可能な項目は、先に次のコマンドを実行し、結果を根拠として使う。
   ```
   ruff check --select D,DOC,ERA,TD,FIX,PGH,RUF100,PLR2044 --preview --output-format=concise <対象>
   ```
   実行できない環境の場合は、その旨を「実行不可」として報告し、目視検査に切り替える。
3. 下記チェックリストを 1 項目ずつ適用する。
4. 出力形式に従って報告する。

## チェックリスト

### A. 機械検査（ruff で判定。ruff の出力をそのまま根拠にする）

| ID | 検査内容 | ruff ルール |
|---|---|---|
| M-01 | コメントアウトされたコードが残っていない | ERA001 |
| M-02 | docstring の有無・書式が規約に準拠している | D1xx–D4xx |
| M-03 | docstring の引数・戻り値・例外が実シグネチャと一致している | DOC1xx–DOC5xx |
| M-04 | TODO の形式（タグ・コロン・説明・Issue リンク）が揃っている | TD001–TD007 |
| M-05 | `# noqa` にルールコードが付いている | PGH004 |
| M-06 | `# type: ignore` にエラーコードが付いている | PGH003 |
| M-07 | 不要になった `# noqa` が残っていない | RUF100 |
| M-08 | 空のコメント行が無い | PLR2044 |

### B. 目視検査（ruff では検出できない。1 コメントずつ判定する）

| ID | 検査内容 | 違反時の分類 |
|---|---|---|
| V-01 | コメントの記述内容が、隣接するコードの実際の挙動と一致しているか | 誤コメント |
| V-02 | コードを逐語的に言い換えただけのコメントでないか | 冗長 |
| V-03 | 説明対象の識別子名の単語を並べただけのコメントでないか | 冗長 |
| V-04 | docstring に、コードを読まずに呼び出すために必要な情報が揃っているか（引数フォーマット、値域、順序の意味、重複時の挙動、永続化先） | 情報不足 |
| V-05 | 副作用がある関数の docstring に、その副作用が明記されているか | 情報不足 |
| V-06 | 事前条件・呼び出し順序の制約がある関数に、その制約が明記されているか | 情報不足 |
| V-07 | 非自明な最適化・回避策・意図的な非効率に、その理由が書かれているか | Why 欠落 |
| V-08 | 採用しなかった代替案がある箇所に、選択理由が書かれているか | Why 欠落 |
| V-09 | 呼び出し側に無関係な実装詳細が docstring に混入していないか | 抽象漏れ |
| V-10 | 変更履歴・更新日・作者名が書かれていないか | アンチパターン |
| V-11 | 区切り線・バナー・位置マーカー・閉じ括弧コメントが無いか | アンチパターン |
| V-12 | 情報を持たないノイズ docstring が無いか（`"""Tests for X."""` 等） | アンチパターン |
| V-13 | 3 段落を超える長文コメントが無いか | 過剰 |
| V-14 | ファイル内でコメント言語・docstring スタイルが統一されているか | 不統一 |
| V-15 | コメントが最終更新されたコミットと、対応コードの最終更新コミットが乖離していないか（`git log -L` で確認可能な場合のみ） | 陳腐化の疑い |

## 判定基準

- **BLOCKER**: V-01（記述が実装と矛盾）、M-03（docstring とシグネチャの不一致）
- **MAJOR**: M-01, M-05, M-06, V-04, V-05, V-06, V-07, V-10
- **MINOR**: 上記以外のすべて
- BLOCKER が 1 件でもあれば総合判定は FAIL、無ければ PASS とする。

## 出力形式

以下の形式のみを出力せよ。前置き・後書き・感想を書いてはならない。

```
## 判定: <PASS または FAIL>

## サマリ
- BLOCKER: <件数>
- MAJOR: <件数>
- MINOR: <件数>
- ruff 実行: <成功 / 実行不可（理由）>

## 指摘一覧

### [<重要度>] <ID> <ファイルパス>:<行番号>
**該当**
```python
<該当コメントとその対象コード（最大 5 行）>
```
**問題**
<1〜2 文。何がどう規約または実装と食い違っているか>

**修正案**
```python
<修正後のコメント。修正案を提示できない場合は「要確認: <確認すべき事項>」と書く>
```

（以降、重要度の高い順に繰り返す）

## 判断を保留した箇所
| ファイル:行 | 保留理由 |
|---|---|
```

## 出力時の制約

1. 指摘は必ず実際のファイル内容に基づくこと。存在しない行番号・存在しないコメントを報告しては
   ならない。
2. コメントの意図が実装から確定できない場合、推測で「誤コメント」と断定してはならない。
   「判断を保留した箇所」に記載すること。
3. 修正案を書く際、元のコメントに含まれていた情報（背景・理由・制約）を落としてはならない。
   短くすることが目的ではない。
4. 指摘が 0 件のチェック項目については、何も出力しない（「問題ありません」も書かない）。
5. コード自体の設計・命名・アルゴリズムに関する指摘は、本レビューの対象外とする。
   コメントと docstring のみを対象とせよ。
