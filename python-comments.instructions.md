---
applyTo: "**/*.py"
---

# Python コメント記述ルール

本ファイルは Python のコメント・docstring を生成／編集する際の遵守事項を定める。
助動詞は RFC 2119 に従う（MUST / MUST NOT / SHOULD / SHOULD NOT / MAY）。

## 0. プロジェクト設定（着手前に確定させること）

| 項目 | 値 |
|---|---|
| コメント・docstring の言語 | `<日本語 / 英語>` |
| docstring 規約 | `<google / numpy / pep257>` |
| コメント密度の方針 | `<最小限（Clean Code 寄り） / 積極的（APOSD 寄り）>` |
| 行長 | コード `<88 / 79>` 文字、コメント・docstring 72 文字 |

上記が未確定の場合、コメントを生成する前に人間に確認すること。値を推測して実装を進めては
ならない（MUST NOT）。

## 1. 最上位の原則

- **P-01 (MUST)** コメントには Why（意図・理由・トレードオフ・制約）を書く。What（コードが何を
  しているか）を書いてはならない。読み手は Python を理解しているが、この実装で何を達成したい
  のかは知らない、という前提で書く。
- **P-02 (MUST)** コードを変更したとき、そのコードに隣接する既存コメントと docstring が依然と
  して正しいかを確認し、矛盾があれば同一の変更内で更新する。矛盾したコメントを残すことは、
  コメントを書かないことより有害である。
- **P-03 (MUST NOT)** 実装から確実に読み取れない意図・背景・経緯を、推測で断定的にコメントとし
  て書いてはならない。根拠が無い場合はコメントを書かず、その旨を人間に報告する。
- **P-04 (MUST NOT)** リファクタリングや修正の際に、人間が書いた既存の Why コメントを削除・要約
  してはならない。削除が妥当と考える場合は、削除せずに理由を人間に提示する。
- **P-05 (MUST)** 同一ファイル内の既存スタイル（命令形／説明形、インデント幅、言語）に合わせる。
  ファイル内の一貫性はグローバルな規約より優先される。

## 2. インライン・ブロックコメント

- **C-01 (MUST NOT)** コードを逐語的に言い換えるコメントを書かない。
  - 悪い例: `x = x + 1  # x をインクリメント`
  - 良い例: `x = x + 1  # 境界分を補正する`
- **C-02 (MUST NOT)** 説明対象の識別子名を構成する単語をそのまま並べただけのコメントを書かない。
- **C-03 (MUST)** ブロックコメントは説明対象のコードと同じインデントに置き、各行を `#` + 半角
  スペース 1 個で始める。段落区切りは `#` のみの行とする。
- **C-04 (MUST)** インラインコメントは文から 2 個以上のスペースを空け、`#` + 半角スペース 1 個で
  始める。
- **C-05 (SHOULD NOT)** インラインコメントを多用しない。原則ブロックコメントを使う。
- **C-06 (MUST)** 次のいずれかに該当する箇所には必ずコメントを書く。
  - 一見冗長・非効率に見えるが、性能・互換性・仕様上の理由で意図的にそうしている実装
  - 外部システム・ライブラリのバグや制約を回避するための実装
  - 採用しなかった代替案があり、その理由が実装から読み取れない箇所
  - 非自明な最適化（なぜそれが正しいのかの根拠を含める）
- **C-07 (MUST NOT)** コメントや `=` を縦に揃えるための余分な空白を入れない。

## 3. docstring

- **D-01 (MUST)** public のモジュール・クラス・関数・メソッドに docstring を書く。ただし次の
  すべてに当てはまる場合は 1 行 docstring で足りる: 公開 API でない／規模が小さい／ロジックが
  自明。
- **D-02 (MUST)** docstring は「その関数のコードを読まずに呼び出しを書ける」情報を含む。
- **D-03 (MUST NOT)** 呼び出し側に無関係な実装の詳細を docstring に書かない。それはコード横の
  コメントに書く。
- **D-04 (MUST)** 次の情報が実装に存在する場合、docstring に必ず記載する。
  - 引数の期待フォーマット・順序の意味・値域
  - 副作用（引数やグローバル状態の変更、I/O、キャッシュ更新）
  - 事前条件・呼び出し順序の制約（例: 単調増加する値のみ渡せる）
  - 同名・重複データが既に存在する場合の挙動
  - 永続化・保存先（メモリ内か、ディスクか、外部サービスか）
- **D-05 (MUST NOT)** シグネチャを docstring に反復しない（`"""func(a, b) -> list"""` は禁止）。
- **D-06 (MUST NOT)** type hints がある引数の型を docstring に重複記載しない。
- **D-07 (MUST)** `Raises:` にはインターフェースとして関係する例外のみ列挙する。docstring に
  記載した仕様に違反した場合に送出される例外は記載しない。
- **D-08 (MUST)** ジェネレータは `Returns:` ではなく `Yields:` を使う。
- **D-09 (MUST NOT)** 新しい情報を持たない docstring を書かない（`"""Tests for foo.bar."""`、
  `"""デフォルトコンストラクタ"""` など）。書くことが無ければ書かない。
- **D-10 (MUST)** 1 行 docstring は開始・終了のクォートを同じ行に置き、前後に空行を入れない。
  複数行 docstring は「要約行 → 空行 → 詳細」とし、閉じクォートを独立行に置く。
- **D-11 (MUST)** 常に `"""` 三重ダブルクォートを使う。バックスラッシュを含む場合は `r"""`。

## 4. 禁止するコメント（アンチパターン）

以下を新規に生成してはならない（MUST NOT）。既存コードに発見した場合は、削除を提案する。

- **A-01** コメントアウトされたコード（履歴は VCS が保持する）
- **A-02** 変更履歴・更新日・チケット番号の羅列（Journal Comments）
- **A-03** 作者名・担当者名の埋め込み（Attributions and Bylines）
- **A-04** 区切り線・バナー・位置マーカー（`# ===== 処理開始 =====` など）
- **A-05** 閉じ括弧・ブロック終端を示すコメント
- **A-06** 情報を持たないノイズコメント（`# ループする`、`# 変数を初期化`）
- **A-07** 全関数への機械的な docstring 付与（規約を満たすためだけの空虚な記述）
- **A-08** 壁のような長文コメント。3 段落を超える場合は設計文書へ切り出し、リンクを張る
- **A-09** そのファイルと無関係な情報（他モジュールの仕様、システム全体の説明）
- **A-10** 説明をすべて超長い関数名に押し込み、コメントを一切書かない書き方

## 5. TODO と抑制コメント

- **T-01 (MUST)** TODO は `# TODO: <Issue URL または Issue ID> - <実施内容>` の形式で書く。
  - 例: `# TODO: https://github.com/org/repo/issues/123 - リトライ間隔を指数バックオフにする`
- **T-02 (MUST NOT)** 個人名・チーム名を文脈として指す TODO を書かない。
- **T-03 (MUST NOT)** `FIXME` `XXX` `HACK` を新規に使わない。`TODO` に統一する。
- **T-04 (MUST)** 「将来〜する」型の TODO には、具体的な日付または具体的なイベント（例:
  「全クライアントが v2 API に移行したら削除」）を含める。
- **T-05 (MUST)** `# noqa` は必ずルールコードを伴わせる（`# noqa: E501`）。
- **T-06 (MUST)** `# type: ignore` はエラーコードを伴わせ、抑制理由がコードから自明でない場合は
  理由を併記する。
- **T-07 (MUST)** 未使用引数を `del` で削除する場合、理由をコメントで書く。

## 6. 自己検証（出力前に必ず実行）

コメントまたは docstring を含むコードを提示する前に、以下をすべて満たすことを確認する（MUST）。

1. 書いたすべてのコメント・docstring の記述が、提示するコードの実装と一致している
2. docstring の `Args:` / `Returns:` / `Raises:` が実際のシグネチャと過不足なく対応している
3. 推測で書いた記述が 1 つも含まれていない
4. §4 のアンチパターンを 1 つも生成していない
5. 既存ファイルのコメントを削除・改変していない（している場合は差分を明示的に報告する）

いずれかを満たせない場合は、コードを提示せずに不足情報を人間に質問する。
