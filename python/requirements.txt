# boto3 は Lambda ランタイムに同梱されているが、DR 切替という用途上
# 挙動を固定したいため明示する。
#
# 1.41.0 以上が必須。GetRestApi の apiStatus は botocore 1.41.0 でモデルに
# 追加されたもので、1.40.x では応答に含まれていてもパース時に落とされる
#（実測で確認）。apigateway の check がこの値を見るため、下回ると確認が
# 黙ってスキップされる。
boto3==1.43.72
kubernetes==31.0.0
