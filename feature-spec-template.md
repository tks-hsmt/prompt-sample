# 機能仕様書テンプレート

このファイルをコピーして、機能ごとに `<feature-name>.md` という名前で保存してください。
**最初に「テンプレート構造ガイド」を読んで全体像を把握**し、その後コメント
(`<!-- ... -->`)に書かれた指示とサンプルに従って各セクションを埋めてください。
最終的にコメントもすべて削除します。

> **凡例**:
> - **【必須】** 削除しないでください。すべての機能で書く必要があります。
> - **【推奨】** 特別な理由がない限り書いてください。
> - **【条件付き】** 該当する条件のときだけ書いてください。
> - **【任意】** 必要に応じて書いてください。
> - **【複数可】** 必要な数だけブロックを繰り返せます。

> **英語のまま使う部分**(変更しないこと):
> - `### Requirement:` `#### Scenario:` のセクション見出し
> - **GIVEN / WHEN / THEN / AND**(BDD のキーワード)
> - **SHALL / MUST / SHOULD / MAY**(RFC 2119 の規範語)
> - **When / While / If / Where / Otherwise**(EARS のパターン識別子)
>
> 上記以外の本文・章タイトルはすべて日本語で書いてください。

---

## テンプレート構造ガイド

テンプレート全体の階層構造と、各セクションの分類は以下の通りです。
**該当しないセクションは丸ごと削除してください**。

```
spec.md
├── YAML frontmatter                          [必須]
├── # <機能名> Specification                  [必須]
├── ## 目的                                    [必須]
├── ## スコープ                                [推奨]
├── ## 用語集                                  [条件付き]    用語が 3 個以上ある場合
├── ## 機能要件                                [必須]
│   └── ### Requirement: <要件名>             [必須・複数可]
│       ├── (要件本文・EARS 形式)              [必須]
│       ├── #### Scenario: <シナリオ名>       [必須・複数可]
│       │   └── GIVEN / WHEN / THEN / AND     [必須]
│       └── #### Rationale                    [任意]
├── ## 非機能要件                              [推奨]
│   ├── ### 性能                              各カテゴリは個別に削除可
│   ├── ### 信頼性 / 可用性
│   ├── ### セキュリティ
│   ├── ### 観測性
│   └── ### 保守性
├── ## インターフェース                        [条件付き]    外部 I/O がある場合
│   ├── ### 入力
│   ├── ### 出力
│   └── ### 設定パラメータ
├── ## データモデル                            [条件付き]    データを永続化する場合
├── ## 制約事項                                [推奨]
├── ## 前提条件                                [条件付き]    暗黙の前提がある場合
├── ## 依存関係                                [条件付き]    他機能・外部依存がある場合
├── ## 対象外                                  [推奨]
└── ## 参考資料                                [推奨]
```

### 編集の進め方

1. **不要なセクションを削除**:【条件付き】【任意】のうち該当しないものを丸ごと削除
2. **frontmatter を埋める**:識別情報を最初に確定
3. **目的を書く**:この機能の Why を 2〜4 文で
4. **機能要件を書く**:Requirement と Scenario を必要な数だけ追加
5. **その他のセクションを埋める**:残ったセクションを上から順に
6. **コメントをすべて削除**:`<!-- -->` を全て取り除いて完成

---

## テンプレート本体

ここから下を `<feature-name>.md` にコピーして編集します。

```markdown
<!--
============================================================================
YAML frontmatter
============================================================================
ファイル冒頭の --- に挟まれた部分。AI とツールがメタデータを読み取る場所。

各フィールドの書き方:
- capability_id: 機能識別子。kebab-case で globally unique。
  例: csv-rule-distribution
- capability_name: 人間が読む名前。日本語可。
  例: CSV ルール配布
- version: SemVer。大きな変更時に上げる。初期は 1.0.0 で OK。
- status: draft / in-progress / stable / deprecated のいずれか。
- last_updated: ISO 8601 形式。編集するたびに更新する。
- owners: 担当者またはチーム名(1 つ以上)。
- related_files: 主要な実装ファイル(3〜5 個まで)。全関連ファイルは書かない。
- depends_on: この機能が呼び出す他機能の capability_id(複数可)。
- consumed_by: この機能を呼び出す他機能の capability_id(複数可)。
- verified_by: 自動テストファイルへのパス。

【サンプル】
---
capability_id: csv-rule-distribution
capability_name: CSV ルール配布
version: 1.0.0
status: stable
last_updated: 2026-05-08

owners:
  - platform-team
related_files:
  - fluentd/plugins/filter_rule_match.rb
  - charts/rsyslog/templates/rules-configmap.yaml

depends_on:
  - s3-storage
consumed_by:
  - fluentd-tag-routing
verified_by:
  - test/plugins/test_filter_rule_match.rb
---
-->
---
capability_id: <feature-name-in-kebab-case>
capability_name: <人間が読む名前>
version: 1.0.0
status: stable
last_updated: 2026-05-08

owners:
  - <担当者名 または チーム名>
related_files:
  - <ファイルパス1>
  - <ファイルパス2>

depends_on:
  - <他の capability_id>
consumed_by:
  - <他の capability_id>
verified_by:
  - <テストファイルパス>
---

<!--
============================================================================
タイトル
============================================================================
"Specification" という英単語が AI に「これは仕様書」というシグナルを与える。

【サンプル】
# CSV ルール配布 Specification
-->
# <機能名> Specification

<!--
============================================================================
目的
============================================================================
2〜4 文で「なぜこの機能が存在するか」を書く。
"何をするか" ではなく "なぜそうあるべきか" を書くのがコツ。

書き方のコツ:
- ビジネス価値・ユーザー価値・技術的必然性のいずれかに必ず紐付ける
- 数値で改善効果を示せると尚良い(「数日から 5 分に短縮」など)
- 過度に詳細にしない(詳細は「機能要件」で書く)

【良いサンプル】
分類ルールを Fluentd 起動時にハードコードせず、S3 上の CSV から動的に取得することで、
ルール変更時の Fluentd 再起動を不要にする。これによりルール更新のリードタイムを
数日から 5 分に短縮し、運用負荷を削減する。

【悪いサンプル】
S3 から CSV をポーリングする機能。
→ 何をするか(What)しか書いていない、なぜ必要かが不明。
-->
## 目的

<ここに 2〜4 文で目的を書く>

<!--
============================================================================
スコープ
============================================================================
この機能が責任を持つ範囲と、持たない範囲を明示する。
両方書くこと(片方だけだと曖昧になる)。

書き方のコツ:
- 「対象範囲」: この機能が責任を持って実装する事項
- 「対象外」: 似ているけれど別の機能や、意図的にやらないこと
- AI に「この変更はこの spec に書くべきか、別の spec か」を判断させる情報

【サンプル】
**対象範囲**:
- S3 からの CSV ダウンロードとメモリ上のルールテーブル更新
- ETag による差分判定で不要な転送を回避
- S3 取得失敗時の継続動作

**対象外**:
- ルール CSV 自体のバリデーション(別機能 `rule-validation` で扱う)
- S3 への書き込み(配布元の管理は別機能 `rule-publishing`)
-->
## スコープ

**対象範囲**:
- <この機能が責任を持つこと>
- <この機能が責任を持つこと>

**対象外**:
- <意図的に責任を持たないこと>
- <意図的に責任を持たないこと>

<!--
============================================================================
用語集
============================================================================
書き方のコツ:
- プロジェクト独自の用語、または業界用語の社内独自の使い方を書く
- HTTP や JSON など業界共通の用語は書かない(ノイズになる)
- 略語の正式名称も含めると親切(例: ETag = Entity Tag)
- 用語が capability 横断で使われるなら、別ファイル(glossary.md)に切り出す

【サンプル】
| 用語 | 定義 |
|---|---|
| ルールテーブル | メモリ上に保持される {pattern, action} のマップ |
| 配布元 CSV | S3 上の `s3://<bucket>/rules.csv` を指す |
-->
## 用語集

| 用語 | 定義 |
|---|---|
| <用語1> | <定義> |
| <用語2> | <定義> |

<!--
============================================================================
機能要件
============================================================================
システムが満たすべき動作を EARS 形式で書く。

EARS の 5 パターン(英語キーワードは厳守):
  1. 常時:        The system SHALL <動作>
  2. イベント駆動: When <トリガー>, the system SHALL <動作>
  3. 状態駆動:    While <状態>, the system SHALL <動作>
  4. オプション:   Where <機能が有効>, the system SHALL <動作>
  5. 異常系:      If <望ましくない状況>, then the system SHALL <動作>

modal verb の使い分け(RFC 2119):
  SHALL / MUST    : 絶対要件。違反すれば仕様違反
  SHOULD          : 強い推奨。違反するなら理由が必要
  MAY             : 任意。実装してもしなくてもよい
  MUST NOT        : 絶対禁止
-->
## 機能要件

<!--
============================================================================
要件ブロック
============================================================================

【Requirement のサンプル(EARS の異なるパターン例)】

例 1: イベント駆動(When 〜)
### Requirement: 定期ポーリング
When 前回のポーリングから 5 分が経過したとき, the system SHALL
S3 オブジェクト `rules.csv` をポーリングする。

例 2: イベント駆動 + 条件分岐(When 〜, Otherwise 〜)
### Requirement: ETag による条件付きダウンロード
When キャッシュ済みの ETag が S3 オブジェクトの現在の ETag と異なるとき,
the system SHALL CSV をダウンロードする。
Otherwise, the system SHALL ダウンロードをスキップする。

例 3: 異常系(If 〜 then 〜)
### Requirement: S3 障害時の継続動作
If S3 アクセスが失敗した場合(ネットワークエラー、認証エラー、5xx 応答),
then the system SHALL キャッシュ済みのルールテーブルでの処理を継続し,
SHALL ERROR ログを出力する。

例 4: 常時(Ubiquitous - キーワードなし)
### Requirement: 構造化ログ出力
The system SHALL すべてのログを timestamp、level、message、capability_id を
含む JSON 形式で出力する。

例 5: 状態駆動(While 〜)
### Requirement: シャットダウン中の処理停止
While the system is in シャットダウン状態, the system SHALL NOT
新しいポーリングサイクルを開始する。

例 6: オプション機能(Where 〜)
### Requirement: メトリクスエクスポート
Where Prometheus エクスポート機能が有効化されているとき, the system SHALL
ポーリング所要時間と取得件数をメトリクスとして公開する。
-->

### Requirement: <要件名>

<EARS 形式で要件本文。SHALL/MUST/SHOULD/MAY のいずれかを必ず含める>

<!--
============================================================================
シナリオ
============================================================================
↑ 上の Requirement に属するシナリオを書く。

具体的な動作例を Given/When/Then で書く。
正常系と異常系を別シナリオに分けると見やすい。

書き方のコツ:
  GIVEN: 前提・初期状態(状態を表す。完了形が自然)
  WHEN:  単一のトリガー(複数アクションは別シナリオに分ける)
  THEN:  期待される結果(外部から観測可能なもの)
  AND:   追加の検証点(任意)

【正常系のサンプル】
#### Scenario: 通常のポーリング

- **GIVEN** システムが稼働中で、前回のポーリングが成功している
- **WHEN** 前回ポーリングから 5 分が経過する
- **THEN** S3 に対して HEAD リクエストを発行し、現在の ETag を確認する

【正常系で複数の検証点があるサンプル】
#### Scenario: ETag が変化している

- **GIVEN** キャッシュ済みの ETag が S3 上のオブジェクトの現在の ETag と異なる
- **WHEN** ポーリングが実行される
- **THEN** CSV 本体がダウンロードされる
- **AND** メモリ上のルールテーブルが原子的に置き換わる
- **AND** 「ルールテーブル更新」の INFO ログが出力される

【異常系のサンプル】
#### Scenario: S3 が 503 を返す

- **GIVEN** システムが有効なキャッシュ済みルールテーブルを持っている
- **WHEN** S3 へのポーリングリクエストが HTTP 503 で失敗する
- **THEN** マッチング処理はキャッシュ済みのルールテーブルで継続される
- **AND** HTTP ステータスを含む ERROR ログが出力される
-->

#### Scenario: <シナリオ名>

- **GIVEN** <前提条件>
- **WHEN** <トリガー>
- **THEN** <期待される結果>
- **AND** <追加の検証点>

#### Scenario: <別のシナリオ名>

- **GIVEN** <別の前提>
- **WHEN** <別のトリガー>
- **THEN** <別の結果>

<!--
============================================================================
根拠 (Rationale)
============================================================================
↑ 上の Requirement の根拠を書く。

書き方のコツ:
- この要件がなぜ必要かを書く。書いておくと「この要件を撤廃して良いか」の
  判断材料になる
- 「過去にこういう障害があった」「こういう要望があった」など、時間的文脈を書く
- 単に要件を言い換えただけにしない

【サンプル 1】
#### Rationale

ポーリング間隔は SLA(ルール反映の遅延 < 10 分)から逆算して 5 分に設定。
これより短くすると S3 リクエストコストが増大し、長くすると SLA を満たせない。

【サンプル 2】
#### Rationale

2025年10月の障害(S3 リージョン障害で Fluentd が停止)を受けた要件。
ルール反映が遅れることより、パイプライン全体の停止を避ける方が優先。
-->

#### Rationale

<なぜこの要件が必要か。背景・経緯・根拠>

---

### Requirement: <次の要件名>

<次の要件本文>

#### Scenario: <次のシナリオ>

- **GIVEN** <前提>
- **WHEN** <トリガー>
- **THEN** <結果>

<!--
============================================================================
非機能要件
============================================================================
機能要件以外の品質特性を書く。
必ず測定可能な数値を含めること(「速い」「使いやすい」は NFR ではない)。

書き方のコツ:
- ID 形式は NFR-<カテゴリ短縮>-<番号>(例: NFR-PERF-1)
- カテゴリは ISO 29148 に準拠

【サンプル】
### 性能 (Performance)
- **NFR-PERF-1**: 1 回のポーリング処理(HEAD + 必要に応じて GET + テーブル置換)は
  30 秒以内に完了 SHALL する。

### 信頼性 (Reliability)
- **NFR-REL-1**: S3 障害が発生しても、Fluentd 本体のパイプラインは MUST NOT 停止する。

### セキュリティ (Security)
- **NFR-SEC-1**: S3 への接続は IAM Role Anywhere を使用 SHALL し、
  アクセスキーをコード内にハードコードしては MUST NOT。

### 観測性 (Observability)
- **NFR-OBS-1**: ポーリング成功時は INFO ログ、失敗時は ERROR ログを構造化形式
  (JSON)で出力 SHALL する。
- **NFR-OBS-2**: ポーリング所要時間と取得件数をメトリクスとして公開 SHALL する。

### 保守性 (Maintainability)
- **NFR-MAINT-1**: フィルタプラグインは単体テストでカバレッジ 80% 以上を SHALL 維持する。
-->
## 非機能要件

### 性能 (Performance)

- **NFR-PERF-1**: <レイテンシやスループットの数値要件>

### 信頼性 (Reliability) / 可用性 (Availability)

- **NFR-REL-1**: <可用性の数値要件、MTTR、リカバリ要件>

### セキュリティ (Security)

- **NFR-SEC-1**: <暗号化、認証、認可、データ保護の要件>

### 観測性 (Observability)

- **NFR-OBS-1**: <ログ、メトリクス、トレースの要件>

### 保守性 (Maintainability)

- **NFR-MAINT-1**: <コード品質、テストカバレッジ、変更容易性の要件>

<!--
============================================================================
インターフェース
============================================================================
書き方のコツ:
- 入力 / 出力 / 設定の 3 区分で書く
- 形式はスキーマへのリンクで十分(全文を書くと陳腐化する)

【サンプル】
### 入力
| 入力元 | 形式 | 備考 |
|---|---|---|
| `s3://<bucket>/rules.csv` | CSV (UTF-8、ヘッダ行あり) | カラム: `pattern,action` |

### 出力
この機能は直接的な出力を持たない。ルールテーブルは下流の `fluentd-tag-routing` が
メモリ参照する。

### 設定パラメータ
| パラメータ | 型 | デフォルト | 説明 |
|---|---|---|---|
| `s3_bucket` | string | (必須) | 配布元バケット名 |
| `s3_key` | string | `rules.csv` | オブジェクトキー |
| `polling_interval_sec` | int | 300 | ポーリング間隔(秒) |
| `s3_region` | string | `ap-northeast-1` | S3 リージョン |
-->
## インターフェース

### 入力

| 入力元 | 形式 | 備考 |
|---|---|---|
| <ソース名> | <形式> | <備考> |

### 出力

| 出力先 | 形式 | 備考 |
|---|---|---|
| <宛先> | <形式> | <備考> |

### 設定パラメータ

| パラメータ | 型 | デフォルト | 説明 |
|---|---|---|---|
| <名前> | <型> | <デフォルト値> | <説明> |

<!--
============================================================================
データモデル
============================================================================
書き方のコツ:
- DDL 全文は書かない(陳腐化が早い)
- 概念モデルと参照リンクで十分

【サンプル】
ルールテーブル(メモリ上の構造):

| フィールド | 型 | 説明 |
|---|---|---|
| pattern | string | マッチング用の正規表現 |
| action | string | マッチ時のアクション(forward / drop / tag_replace) |
| priority | int | マッチング優先度(数値が小さいほど先に評価) |

CSV スキーマの詳細は `docs/schema/rules-csv.md` を参照。
-->
## データモデル

<データ構造の概念モデルまたはスキーマへの参照>

<!--
============================================================================
制約事項
============================================================================
書き方のコツ:
- 物理制約(リソース上限など)
- 規約上の制約(特定ライブラリ禁止など)
- Constitution(プロジェクト原則)違反になる選択肢を書く
- 理由を必ず併記する(単なるルール羅列にしない)

【サンプル】
- ルールテーブルを in-memory にハードコードすることは禁止する
  (理由: constitution の「設定はコードと分離」原則に違反)
- ETag ヘッダが取得できない S3 互換ストレージはサポート外
  (理由: 差分判定の実装が成立しない)
- ポーリング間隔を 60 秒未満に設定してはならない
  (理由: S3 リクエストコストが急増する)
-->
## 制約事項

- <制約>(理由: <なぜこの制約があるか>)
- <制約>(理由: <なぜこの制約があるか>)

<!--
============================================================================
前提条件
============================================================================
書き方のコツ:
- 環境的な前提(ネットワーク、権限、依存サービスの状態)
- データ的な前提(タイムゾーン、文字コード、フォーマット)
- 「常識」と思える前提でも、暗黙だと AI が誤解する場合は明示する

【サンプル】
- S3 への IAM ロール権限(`s3:GetObject`、`s3:HeadObject`)が Pod に付与されている
- S3 リージョンと Fluentd Pod のリージョンが同一(クロスリージョンは未検証)
- ルール CSV は UTF-8 でエンコードされている(他のエンコーディングはサポート外)
-->
## 前提条件

- <この仕様が前提とする条件>
- <この仕様が前提とする条件>

<!--
============================================================================
依存関係
============================================================================
書き方のコツ:
- frontmatter の depends_on / consumed_by と内容を整合させる
- upstream / downstream / external を区別する

種別の使い分け:
  upstream:   この機能が呼び出す側(なくなると動かない)
  downstream: この機能を呼び出す側(なくなっても動くが利用者がいない)
  external:   外部ライブラリ・SaaS など

【サンプル】
| 依存先 | 種別 | 備考 |
|---|---|---|
| `s3-storage` | upstream | 配布元 |
| `fluentd-tag-routing` | downstream | ルールテーブルの利用者 |
| AWS SDK for Ruby | external | バージョン 3.x 系 |
-->
## 依存関係

| 依存先 | 種別 | 備考 |
|---|---|---|
| <名前> | upstream / downstream / external | <備考> |

<!--
============================================================================
対象外
============================================================================
書き方のコツ:
- 「やらないこと」と「なぜやらないか」を併記する
- 「将来検討」は References に Issue リンクで書く(ここに書くと「やらない」の意味)
- 似た機能と混同されそうな事項に特に有効

【サンプル】
- リロード中に受信したレコードが新旧どちらのルールで処理されるかの保証
  (理由: 原子性は table swap 単位のみで実装している)
- ルール CSV 自体のバリデーション
  (理由: 別機能 `rule-validation` で扱うため二重実装を避ける)
- ルール変更履歴の保持
  (理由: S3 の versioning 機能で代替する)
-->
## 対象外

- <意図的にやらないこと>(理由: <なぜか>)
- <意図的にやらないこと>(理由: <なぜか>)

<!--
============================================================================
参考資料
============================================================================
書き方のコツ:
- ADR (Architecture Decision Record) があれば優先的に列挙
- Issue / PR はマージ済みのものを列挙(議論中は陳腐化する)
- 内部 Wiki / 外部規格書 / RFC へのリンクも有効

【サンプル】
- ADR-0042: ルール配布方式の選定(S3 vs ConfigMap)
- 障害報告書: 2025年10月 S3 リージョン障害(internal)
- AWS S3 ETag 仕様: https://docs.aws.amazon.com/AmazonS3/latest/API/API_Object.html
- 関連 Issue: GitHub PR #1234(初期実装)、PR #1567(ETag 対応追加)
-->
## 参考資料

- <関連 ADR、Issue、ドキュメントへのリンク>
- <関連 ADR、Issue、ドキュメントへのリンク>
```

---

## OpenSpec を使う場合の運用

OpenSpec の `/opsx-propose` でこのテンプレート構造の spec.md を生成させたい場合、
`openspec/config.yaml` の `rules.specs` に以下のような指示を書きます。

```yaml
schema: spec-driven

context: |
  Language: Japanese
  すべての成果物は日本語で作成してください。
  ただし以下は英語のままにしてください:
  - "### Requirement:" "#### Scenario:" のセクション見出し
  - GIVEN / WHEN / THEN / AND
  - SHALL / MUST / SHOULD / MAY
  - When / While / If / Where / Otherwise

rules:
  specs:
    - YAML frontmatter を必ず含める(capability_id、capability_name、version、status、last_updated、owners、related_files)
    - 関係性がある場合は frontmatter に depends_on / consumed_by / verified_by を追加する
    - "## 目的" には 2〜4 文で「なぜこの機能が存在するか」を書く(What ではなく Why)
    - "## スコープ" には対象範囲と対象外を両方書く
    - 機能要件は EARS 形式で書く(Ubiquitous / Event-Driven / State-Driven / Optional / Unwanted Behavior)
    - SHALL / MUST / SHOULD / MAY を RFC 2119 に従って使い分ける
    - 各 Requirement には Scenario を 1 つ以上含める
    - 複雑な要件には #### Rationale セクションで根拠を書く
    - 非機能要件は性能・信頼性・セキュリティ・観測性・保守性のカテゴリで分類する
    - すべての要件には測定可能な数値を含める(「速い」「使いやすい」は不可)
    - 外部システムとの I/O がある場合は "## インターフェース" セクションを書く
    - "## 制約事項" には理由も併記する
    - "## 対象外" には「やらないこと」とその理由を書く
```

これで `/opsx-propose` を実行すると、AI は config.yaml の `rules.specs` を参照して、
テンプレート準拠の spec.md を生成します。delta(`## ADDED Requirements` 等)構造は
OpenSpec が変更時に自動で扱うので、Takeshi さんが直接書くのは「現在の真実」としての
spec.md だけです。

## OpenSpec を使わない場合の運用

`docs/features/` 等にこのテンプレートで spec.md を直接配置し、
`.github/copilot-instructions.md` または `CLAUDE.md` に以下のような指示を書きます:

```markdown
## 機能仕様の参照

このプロジェクトの既存機能の仕様は `docs/features/` 配下に配置されています。

機能の追加・変更・調査を依頼されたら、以下を必ず実行してください:
1. `docs/features/` 配下を確認し、関連する機能のファイルを読む
2. その仕様(特に SHALL / MUST 文)に従って提案・実装を行う
3. 「Scenario」を実装の動作確認基準として扱う
4. 仕様にない動作を実装する場合は、先に仕様の更新を提案する
```

これだけで、OpenSpec なしでも AI は仕様を参照するようになります。

---

## このテンプレートの根拠

- 構造の `### Requirement:` / `#### Scenario:` 表記は [OpenSpec 公式](https://github.com/Fission-AI/OpenSpec/blob/main/docs/concepts.md) の format に準拠
- EARS の 5 パターンは [Alistair Mavin 公式](https://alistairmavin.com/ears/) より
- SHALL / MUST / SHOULD / MAY の使い分けは [RFC 2119](https://www.rfc-editor.org/rfc/rfc2119) より
- GIVEN / WHEN / THEN は [Gherkin 公式](https://cucumber.io/docs/gherkin/) より
- 非機能要件のカテゴリは [ISO/IEC/IEEE 29148:2018](https://drkasbokar.com/wp-content/uploads/2024/09/29148-2018-ISOIECIEEE.pdf) より
